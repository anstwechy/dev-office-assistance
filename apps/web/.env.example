# API base (empty in dev = same origin; Vite proxies /api to the API)
VITE_API_BASE_URL=

# Microsoft 365: tenant and client IDs are provided by the API (database or server M365_* env).
# No VITE_ Azure variables are required in the web bundle.
